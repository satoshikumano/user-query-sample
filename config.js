module.exports = {
    // Replace it with your app id.
    appID : '__APP_ID__',
    // Replace it with your app key.
    appKey : '__APP_KEY__',

    // Replace it with your host name based on location.
    // api.kii.com (US)
    // api-cn3.kii.com (CN)
    // api-sg.kii.com (SG)
    // api-eu.kii.com (EU)
    // Private location: ask.
    appHost : 'api-jp.kii.com',

    // Replace it with Obtained admin token.
    adminToken : '__ADMIN_TOKEN__'
}
